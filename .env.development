# Development Environment Configuration

# Mock Mode
# Enable mock mode for development (use mock data instead of real API)
# Values: 'true' | 'false'
# Default: auto-enabled in development mode if not explicitly set to 'false'
VITE_USE_MOCK=true
